-- ── Programas por defecto ───────────────────────────────────
-- Centralizados aquí para reutilizarlos en keybinds y autostart.

return {
    terminal    = "kitty",
    fileManager = "dolphin",
    menu        = "hyprlauncher",
}
