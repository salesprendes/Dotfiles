import qs.Components
import qs.Config

// Papelera de fila: quitar una nota, un instinto, un servidor, una
// conversación. La misma en el panel y en los ajustes.
IconButton {
    icon: "󰩹"
    diameter: Theme.dp(22)
    iconPixelSize: Theme.sp(11)
    baseColor: "transparent"
    hoverColor: Theme.red
}
