import QtQuick
import QtQuick.Layouts
import qs.Config

// Separador vertical fino entre secciones de una píldora de la barra.
Rectangle {
    Layout.alignment: Qt.AlignVCenter
    implicitWidth: Theme.hairline
    implicitHeight: Theme.dp(14)
    color: Theme.overlay
}
